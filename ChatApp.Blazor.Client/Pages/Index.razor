@page "/"
@attribute [Authorize]
@inject NavigationManager Navigation

<PageTitle>ChatApp</PageTitle>

@code {
    protected override void OnInitialized()
    {
        // Redirect all users to messages section after login
        Navigation.NavigateTo("/messages", replace: true);
    }
}
